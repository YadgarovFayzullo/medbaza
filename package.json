{
  "name": "medbaza",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace medbaza-web",
    "build": "npm run build --workspace medbaza-web",
    "lint": "npm run lint --workspace medbaza-web",
    "typecheck": "npm run typecheck --workspace medbaza-web",
    "test": "npm run test --workspace medbaza-web",
    "generate:api": "npm run generate:api --workspace medbaza-web",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
